type User {
    id: ID!
    name: String!
    jwt: String
}

type Mutation {
    signup(email: String!, password: String!, name: String!): User
    signin(email: String!, password: String!): User
}

type Query {
    hi: String
}