type Post {
  id: ID! @unique
  createdAt: DateTime!
  updatedAt: DateTime!
  caption: String
  # pictureUrl: String!
  user: User!
}

type User {
  id: ID! @unique
  email: String! @unique
  password: String!
  name: String!
  posts: [Post!]!
}